// vehicle_information_screen.dart

import 'package:flutter/material.dart';

class VehicleInformationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Vehicle Information Screen'),
    );
  }
}
